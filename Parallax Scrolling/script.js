// Add the parallax effect to the header and footer elements
const header = document.querySelector('.header');
header.classList.add('parallax');

const footer = document.querySelector('.footer');
footer.classList.add('parallax');
